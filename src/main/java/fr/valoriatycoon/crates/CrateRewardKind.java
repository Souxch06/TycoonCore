package fr.valoriatycoon.crates;

/** Persisted generic crate reward families. Random choices are resolved before persistence. */
public enum CrateRewardKind {
    MONEY_BAG,
    COIN_BAG,
    UNIVERSAL_COIN_BAG,
    XP_VIAL,
    RESOURCE_BUNDLE,
    VANILLA_ITEM,
    CRATE_KEYS,
    PET_KEYS,
    GENERATORS
}
